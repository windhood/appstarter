<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Pawdrop - <%= content_for?(:title) ? 
  content_for(:title) : Settings.tagline -%></title>
  <meta name="description" content="<%= Settings.tagline %>">
  <meta name="author" content="Kristjan Rang">

  <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
  <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
  <![endif]-->

  <link rel="shortcut icon" href="/favicon.ico">
  <link rel="apple-touch-icon" href="/apple-touch-icon.png">

  <%= csrf_meta_tags %>
  <%= stylesheet_link_tag "application" %>
  <%= stylesheet_link_tag "static" %>
</head>
<body>
  <div class="topbar">
    <div class="fill">
      <div class="container">
        <a class="brand" href="/"><%= image_tag("brand.png") %></a>
        <ul class="nav">
          <%= topbar_link "Home", "/" %>
          <%= topbar_link "About", about_path %>
        </ul>
        
        <%= render partial: "shared/topbar_user" %>
      </div>
    </div>
  </div>

  <div class="container">
    <%= yield %>

    <%= render partial: "shared/footer" %>
  </div>

  <%= javascript_include_tag "static" %>
  <%= render partial: 'shared/bottom_include' %>
</body>
</html>